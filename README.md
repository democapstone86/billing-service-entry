# Billing Services Portal - Home Page

This zip contains a standalone HTML implementation of the Billing Services Portal Home page.

## Contents

- `index.html` – Fully self-contained page (HTML + CSS + JS) with:
  - Sticky header with logo placeholder, theme toggle, and user settings menu
  - Light/Dark mode support (using CSS variables and localStorage)
  - Intro card that explains the portal
  - Four accordions:
    - Billing Performance Tiers
    - Billing Services Entry
    - Project Administration
    - Standards

## How to run

1. Extract the zip.
2. Open `index.html` in any modern web browser (Chrome, Edge, Firefox, Safari).
3. Use the theme toggle in the header to switch between light and dark mode.
